// some configs
pub const maxBugs: usize = 8;
pub const maxIn: usize = 10;
pub const maxOut: usize = 6;
pub const maxHidden: usize = 14 * 14;
pub const maxCons: usize = maxHidden;

pub const maxMutRate: u8 = 255;
