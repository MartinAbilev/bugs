// some configs
pub const maxBugs: usize = 16;
pub const maxIn: usize = 7;
pub const maxOut: usize = 6;
pub const maxHidden: usize = 8 * 8;
pub const maxCons: usize = maxHidden;

pub const maxMutRate: u8 = 10;
